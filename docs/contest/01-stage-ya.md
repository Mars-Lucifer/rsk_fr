# Этап «Я» 🟢 собран

Индивидуальная часть конкурса: 8 видеоуроков, к каждому — задание в тренажёре
МАЯК ОКО. Уроки открываются по очереди: следующий доступен, когда закрыт
предыдущий.

Обновлено: 06.08.2026

---

## 1. Состав этапа

| № | Урок | Формат задания |
|---|------|----------------|
| 1 | Вводный урок | Старт |
| 2 | Текст | Текст |
| 3 | Аудио | Аудио |
| 4 | Изображение | Изображение |
| 5 | Интерактив | Интерактив |
| 6 | Видео | Видео |
| 7 | Данные | Данные |
| 8 | Заключительный урок | Итог |

Уроки 2–7 один в один ложатся на `YA_FORMAT_TYPES` из тренажёра — те же шесть
форматов контента.

---

## 2. Поток участника

```
регистрация → письмо → подтверждение → вход
   └─ сразу попадает в раздел «Конкурс» (/cours)
        └─ 8 карточек сеткой 4×2, доступен только следующий урок
             └─ «К уроку» → тренажёр открывается на этом уроке
                  ├─ лесенка уроков в шапке, переключение без перезагрузки
                  ├─ карточка задания + «Задание целиком» в попапе
                  ├─ видеоурок (обложка, плеер по клику)
                  ├─ поля МАЯК ОКО и генерация промта — как обычно
                  └─ ответ: текст или файл до 20 МБ
                       └─ «Завершить задание» → урок закрыт, открыт следующий
```

После восьмого урока — возврат к списку. Дальше пока ничего: см.
[02-stage-my.md](02-stage-my.md).

---

## 3. Архитектура: конкурс — потребитель тренажёра

Главное ограничение: тренажёр в проде, им пользуются прямо сейчас. Конкурс не
меняет его поведение для существующих сценариев.

```
┌─ КОНКУРС ──────────────────────────────────┐
│  /cours — уроки, прогресс, гейты           │
│  привязка к профилю портала                │
└───────────────┬────────────────────────────┘
                │  контекст доступа { tokenType, sectionId, taskRange }
┌───────────────▼────────────────────────────┐
│  ТРЕНАЖЁР — как есть                       │
│  legacy-токены · сессии · админка · СОВА   │
└────────────────────────────────────────────┘
```

**Точка стыковки одна:** `resolveMayakTokenContext()` в
[mayakTokenContext.js](../../src/lib/mayakTokenContext.js) получил четвёртую
ветку `tokenType: "contest"` рядом с `legacy`, `session` и `bypass`.

Что делает конкурсный вход:

- «ключом» служит авторизация портала — участнику ничего не выдаётся;
- ключ вида `contest-3` только указывает урок, секретом не является;
- ничего не расходуется: `usedCount` не трогается;
- `sessionId` пустой → конкурсные прохождения не создают сессий и не попадают
  в сессионный дашборд, статистику столов и очередь инспектора.

Ветка срабатывает только на префикс `contest-`. Откат = удалить ветку.

### Конкурсный режим экрана

При заходе из урока тренажёр выглядит иначе:

| Обычный режим | Конкурсный |
|---|---|
| «Завершить сессию» | «Вернуться к урокам» |
| переключатель номеров задач | лесенка из 8 уроков |
| «Выбрать роль» | скрыта — роли относятся к этапу «Мы» |
| история запросов | скрыта |
| материалы по таймеру сессии | доступны сразу |

Режим включается **явно**: переход из списка идёт с меткой `?contest=1` и
ставит флаг в `sessionStorage`. По куке ориентироваться нельзя — она живёт
30 дней, и тогда обычный заход через меню «Маяк Око» тоже открывал бы
конкурсный экран.

---

## 4. Как заливается контент

Три разных канала, у каждого своё хранилище. Не перепутать.

| Что | Где лежит | Как заливать |
|-----|-----------|--------------|
| Видеоурок | `learning_service`, `courses.download_url` | ссылка на RuTube-embed |
| Название и описание урока | `learning_service`, `courses` | там же |
| Текст задания (цель, шаги, результат) | **код**: [contestTrainerTasks.js](../../src/lib/contestTrainerTasks.js) | временно, переедет в БД |
| Ответы участников | `data/contest-answers/` | загружают сами участники |

Колоды тренажёра в этот список не входят: в конкурсе карт и колод нет
(решение от 06.08.2026, см. пункт 1а). Материалы к заданию, если понадобятся,
надо будет привязывать к уроку, а не к номеру задачи в колоде.

### Видео грузится только по клику

Замер: embed RuTube тянет **~2 МБ своего JS** плюс 271 КБ метрики Яндекса на
каждый вставленный ролик. Восемь уроков — это двадцать мегабайт внешних
запросов. Поэтому до клика показывается обложка, плеер создаётся по нажатию.
Место зарезервировано пропорцией 16/9, подмена не сдвигает вёрстку.

Своей картинки-обложки у уроков пока нет: у RuTube превью не забрать, его API
и oEmbed отвечают 403. В [VideoFacade](../../src/components/ui/VideoFacade.js)
готов проп `poster` — когда появится канал загрузки обложек, достаточно
передать ссылку.

---

## 5. Проверено

Сквозной прогон на локальном стенде (06.08.2026):

- регистрация нового участника → письмо в MailHog → подтверждение → вход;
- профиль создаётся событием `user.created` через RabbitMQ;
- 8 уроков закрыты через реальные ручки, 8 ответов сохранено;
- лесенка и список показывают одинаковые состояния;
- заход через меню «Маяк Око» открывает обычный тренажёр, не конкурсный.

---

## 6. Что осталось незакрытым ⬜

| # | Что | Тип |
|---|-----|-----|
| 1 | Контента нет: один промо-ролик на всех уроках, тексты заданий — рыба | контент |
| ~~1а~~ | ~~Конкурс ездит на колоде тренажёра~~ — 🟢 закрыто 06.08.2026, см. ниже | код |
| 2 | Критерий «задание выполнено» не определён: «Завершить» верит участнику | решение + код |
| 3 | Флаг `is_learned` переключает фоновая задача раз в час → «Команды» и «Проекты» откроются с задержкой | код |
| 4 | Нет финального экрана «этап «Я» пройден» и перехода к «Мы» | код |
| 5 | Лесенка уроков прячется на экранах уже 1200px | вёрстка |
| 6 | Тексты заданий живут во фронте, а не в БД | код |

Пункт 1 закрывается вами, остальные — разработкой.

### Пункт 1а: конкурс отвязан от колод 🟢

**Решение от 06.08.2026: в конкурсе колод и карт нет.** Это другой формат.
В тренажёре участник разбирает готовые карточки заданий из колоды; в конкурсе
он решает одну задачу своего урока. Общее у них ровно одно — фреймворк
МАЯК ОКО как инструмент: семь полей, сборка промта, отправка в нейросеть.

Что это значит для кода. Сейчас связь с колодой держится в одном месте:

```
buildContestAccessContext(lessonNumber)   ← src/lib/mayakContestAccess.js
   └─ отдаёт тренажёру sectionId + taskRange
        └─ useMayakTaskManager грузит /api/mayak/content-bundle
             └─ тренажёру нужна «текущая задача», иначе экран пустой
```

Колода при этом участнику не показывается: панель урока
[ContestLessonPanel](../../src/components/features/tools-2/ContestLessonPanel.js)
рисует цель, шаги, результат и приём ответа — ни карточек, ни номеров задач,
ни файлов колоды. То есть колода нужна только чтобы тренажёр не остался без
задачи.

Проверка 06.08.2026 показала, что и эта подпорка работает неверно: карта
режет общую колоду `1-100` блоками по 4 задачи, а форматы в ней идут своим
порядком — 7 уроков из 8 открывали задачу не того формата (урок «Аудио» →
текстовые задачи). Чинить разметку смысла нет: колода уходит целиком.

**Что остаётся без изменений** (решение от 06.08.2026): весь остальной экран
тренажёра. Поля МАЯК ОКО, сборка промта, отправка в нейросеть, оценка СОВЫ,
ролик урока — всё это в конкурсе нужно и работает. Уходит только источник
задачи.

### Как сделано

Тренажёр берёт задачи одним запросом:
`GET /api/mayak/content-bundle?sectionId=<slug>` → `{ tasks, texts, meta }`.
Дальше `useMayakTaskManager` раскладывает их по номерам, опираясь на
`meta.rangeStart` / `meta.rangeEnd`, если они есть, и только иначе разбирает
номер из самого слага.

Отсюда самый дешёвый шов — сам эндпоинт:

1. `content-bundle` получает ветку на префикс `contest-`: вместо чтения диска
   собирает бандл из одного задания урока, с `meta.rangeStart = rangeEnd = 1`.
2. `contestTrainerTasks.js` перестаёт хранить `sectionId` и `taskRange` — он
   хранит само задание урока.
3. `buildContestAccessContext` отдаёт `sectionId: "contest-<номер урока>"`.

Тренажёр, хуки и панель урока не изменились: бандл приходит той же формы.
Колода в конкурсе больше не участвует.

Одна оговорка вскрылась на прогоне: правило «оценка недоступна на первых трёх
заданиях колоды» — правило колоды (там это выбор роли, анкета и тест).
Конкурсная задача всегда первая, поэтому оценка СОВЫ оказалась бы закрыта на
всех восьми уроках. В конкурсном режиме правило отключено.

Когда задание переедет в БД (пункт 6), ветка в `content-bundle` начнёт брать
текст оттуда — форма ответа останется прежней.

### Прогон 06.08.2026

Сквозной проход участником на локальном стенде: регистрация → письмо
в MailHog → подтверждение → вход. После входа сразу `/cours`, 8 карточек,
открыт только первый урок.

Проверено на уроке 1: задание пришло из урока, а не из колоды; поля МАЯК ОКО
заполняются, промт собирается; кнопка оценки СОВЫ активна; ответ сохранён
в `data/contest-answers/`; «Завершить задание» закрыло урок (`user_progress`
в `rsk_learning`) и открыло второй; переключение на урок 2 подтянуло его
задание. Ошибок в консоли от конкурсного потока нет.

Колоды тренажёра при этом читаются по-прежнему: `content-bundle` на `1-100`
отдаёт колоду с диска, ветка срабатывает только на префикс `contest-`.

### Состояние локального стенда на 06.08.2026

- в `rsk_learning` заведены 8 уроков, у всех восьми одинаковый `download_url`
  (один промо-ролик);
- `user_progress` и `submissions` пусты, участников 0 — данные прошлого
  прогона стёрты, `data/contest-answers/` отсутствует;
- активный источник контента — `public/tasks-2/v2/`: `data/mayak-content/`
  не существует, `MAYAK_CONTENT_DIR` не задан. Для прода это надо проверить
  отдельно, иначе админка и тренажёр смотрят в разные места.
