# Что сделано по конкурсу

Новое — сверху. Строка на изменение: что и зачем, с коммитом.

---

## 17.08.2026

- заведён `12-concept.html` — итоговый документ для руководства: вся концепция
  на одной странице из одиннадцати блоков (момент, конструкция, польза
  заинтересантам, измерение, год, люди, события, масштаб, дорожная карта,
  риски, четыре решения). Самодостаточный HTML без внешних зависимостей,
  печатается в PDF; шкала «ЗВЕЗДА» нарисована инлайновым SVG как измерительный
  прибор с делениями уровней и примером профиля колледжа
- заведён `11-evidence-grid.md`: первое наполнение сетки доказательств —
  что означают среда, деятельность и сознание в каждом из шести лучей и какими
  артефактами подтверждаются; луч «Знания и навыки» развёрнут по шести уровням
  как образец языка; список типовых подмен, которые доказательством не являются
  (приказ вместо процесса, скриншот вместо выгрузки, договор поставки вместо
  пользования); дешёвый способ проверки слоя сознания. Числовые пороги
  сознательно не заданы — вводятся во втором цикле по распределению
- заведён `10-index-and-tracks.md`: конструкция ИЦЗ (геометрическое среднее по
  шести лучам вместо минимума — минимум остаётся флагом «узкое место»; научная
  основа — Handbook OECD/JRC по составным индексам, прецеденты EGDI с
  Z-стандартизацией, DESI, переход HDI на геометрическое среднее), панель
  отслеживаемых показателей включая метрики здоровья системы, три рейтинга и
  дивизионы по стартовому уровню, год как сезон с разбором игровых механик
  (что берём из live-service, что выбрасываем — прежде всего сгорание наград),
  ступени Мастера и асессора с ревалидацией, три форума под три контура.
  Правило свода в `09-roadmap.md` §7 уточнено со ссылкой сюда
- в `09-roadmap.md` добавлен §9а «Методологические подходы»: концепция цикла
  (организация учится, делая свою работу, и предъявляет доказательство), три
  вложенных контура одного строения — участник/команда/организация, разбор
  семи подходов механизма изменения из руководства Мастера в применении к году,
  шесть подходов измерения, пять подходов масштабирования и четыре пробела,
  которые надо закрыть (отстающие, межволновое удержание, тиражирование чужого
  решения, передача методологии)
- заведён `09-roadmap.md`: дорожная карта системы на масштаб ~3 600 колледжей.
  Заинтересанты и их выгода (регион отвечает за отраслевую цифровую зрелость по
  ПП № 58, инструмента измерения на уровне колледжа СПО нет — это и есть наша
  ниша), роль оператора шкалы вместо подрядчика, роли и статусы (директор —
  владелец уровня, асессор, Мастер), годовой цикл из шести волн по лучам,
  повторяющий устройство 8-часового МАЯКа, два несмешиваемых контура баллов
  («ОП мотивируют, ИЦЗ решает»), схема достоверности на массовом входе
  (срез всем, подтверждение — асессорами и выборочным аудитом), фазы 0–4
  с условиями перехода
- заведён `08-icz-contest-concept.md`: концептуальное ядро версии конкурса,
  где измеряется цифровая зрелость организации по лучам «ЗВЕЗДЫ», а не сумма
  сданных заданий. Разобраны положение «Команда цифрового прорыва» (срок
  06.10.2025 — апрель 2026 истёк, восемь расхождений с кодом), ЗВЕЗДА-7 как
  ИЦЗ (6 лучей × 6 уровней), руководство Мастера МАЯКа и мировые практики
  (SELFIE/DigCompOrg, Jisc DET, EFQM/Baldrige, ПРОФ-IT). Найдено: в
  `orgs_service` уже есть поля под шесть лучей и сводный `star`, по нему
  сортируется список организаций, но **никто их не считает** — под индекс
  готово хранилище без источника значений; поле `knowledge_skills_v` названо
  ошибочно (по смыслу «Внешние взаимодействия»)
- заведён `07-engagement-mechanics.md`: разбор внешнего документа «механики
  для КЦП» (аналитика по чужому проекту МЕДИАКАДРЫ) и перенос его выводов на
  конкурс. Статус — видение, ни одно решение не меняется. Главное из разбора:
  рекомендованное автором ядро «Биржа заданий» у нас уже написано (раздел
  «Проекты»), не хватает не кода, а задач и приёмки; наш этап «Мы» по устройству
  совпадает с самым игровым и самым недолговечным вариантом автора

## 11.08.2026

- фронт под ссылки-приглашения: блок приглашения у лидера, страница
  `/teams/invite/[token]`, исключение участника, русские тексты отказов.
  Открытая кнопка «Вступить» со страницы команды убрана
- бэкенд: команда собирается внутри одной организации — участник чужого
  колледжа больше не вступит
- заведена витрина `06-artifacts.md`: какие экраны готовы и что на них видно

## 06.08.2026

- вернулось ядро конкурса отдельным файлом `00-core.md`: словарь, субъекты,
  путь участника целиком, единый список гейтов, карта владения сервисами.
  При разбиении `contest-core.md` по файлам содержание сохранилось, а связки
  между частями потерялись — теперь они снова в одном месте
- этап «Мы» перестал быть белым пятном: тренажёр в нём не участвует, механика
  берётся из существующего раздела «Проекты» — шесть категорий совпадают
  с направлениями «ЗВЕЗДЫ», задания закреплены за командой, сдача и модерация
  уже есть. Разобрано по коду, записано в `02-stage-my.md`
- решения: команда работает только полным составом 4, только внутри одной
  организации, рейтинг нужен, передача лидерства не нужна, переполненной
  команде показываем заглушку, перепроходить закрытый урок нельзя
- бэкенд: вход в команду только по ссылке-приглашению, гейты «прошёл этап «Я»»
  и «указал организацию», исключение участника лидером, мгновенный `is_learned`
  после последнего урока. Ветка `feat/contest-team-invites` в `../back/RSK_back`,
  локально, на прод не выкатывалось. Попутно исправлен падавший ручной
  пересчёт статуса обучения. Фронта под это пока нет
- ревизия команд по коду `teams_service`: состав 3+1 **уже проверяется** при
  вступлении — прежняя запись в `03-teams-and-orgs.md` об обратном была ошибкой.
  Решено: вход в команду только по ссылке-приглашению, и только для тех, кто
  закрыл этап «Я» и указал организацию; лидеру нужна возможность исключать
- конкурс отвязан от колод: `content-bundle` на префикс `contest-` собирает
  бандл из задания урока вместо чтения колоды с диска. Заодно снято правило
  «оценка недоступна на первых трёх заданиях колоды» — конкурсная задача всегда
  первая, и без этого СОВА была бы закрыта на всех восьми уроках. Проверено
  сквозным прогоном участника на локальном стенде
- решение: остальной экран тренажёра в конкурсе сохраняется целиком — поля
  МАЯК ОКО, промт, нейросети, оценка СОВЫ, ролик. Материалов к заданию, скорее
  всего, не будет. План отвязки от колод описан в `01-stage-ya.md`, §1а
- решение: **в конкурсе колод и карт нет**, задание на урок одно. Расхождение
  форматов, найденное ревизией, чинить не нужно — колода уходит целиком.
  Критерий «задание выполнено» отложен до появления материалов уроков
- ревизия готовности этапа «Я» по факту: сверены карта уроков, колода `1-100`
  и база `rsk_learning`. Найдено расхождение форматов — 7 уроков из 8 открывают
  задачу не того формата (пункт 1а в `01-stage-ya.md`)
- `64d911f` — ролики не грузятся, пока их не открыли: замер показал ~2 МБ JS
  на каждый embed RuTube плюс 271 КБ метрики Яндекса
- `29bcbac` — материалы задания (PDF, инструкция, карта) доступны в конкурсном
  режиме: раньше доступ открывался таймером сессии, которого в конкурсе нет
- документация конкурса разложена по файлам в `docs/contest/`

## 05.08.2026

- `b1802d6` — «Маяк Око» из меню больше не открывает конкурсный экран: режим
  включается меткой `?contest=1`, а не долгоживущей кукой
- `928906f` — грузим только нужные плееры вместо восьми сразу
- `d5cbb62` — убрана промежуточная страница урока, ролик стал заставкой карточки
- `006afa7` — список уроков согласован с тренажёром, сетка 4×2; заодно убрана
  старая логика, которая показывала четыре карточки вместо восьми
- `d3f1ccc` — крестик в попапе и короткие подписи в лесенке
- `d98046c` — переход между уроками без перезагрузки, карточка задания
- `7e92a8e` — лесенка уроков переехала в шапку, видеоурок открыт сразу
- `68689a9` — конкурсный режим тренажёра: лесенка уроков вместо управления сессией
- `850cc13` — вход в тренажёр по авторизации портала, без ключа
- `390f940` — задание урока выполняется в тренажёре
- `a96c341` — вход участника ведёт в конкурс, починен выход из аккаунта
- `87cb34f` — сессионная кука работает на локальном стенде
- `4d6d976` — базовый адрес бэкенда вынесен в `RSK_API_BASE`: до этого
  локальный dev-сервер читал и писал боевые данные

### Тогда же, вне кода

- поднят локальный стенд: 6 сервисов, 6 баз, шлюз вместо Traefik, MailHog
- изучен прод: 926 пользователей, 1 тестовый курс, бэкапов нет
- подключён бэкенд-репозиторий `../back/RSK_back`
